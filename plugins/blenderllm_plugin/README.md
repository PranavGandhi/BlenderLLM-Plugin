# BlenderLLM-Plugin

This directory contains the Blender add-on source. The installable add-on is
built by `scripts/build_blender_plugin.py`, which bundles this plugin with the
shared core package from `packages/blenderllm_plugin_core`.

Install the generated zip from:

```text
dist/blenderllm_plugin-0.9.0.zip
```
